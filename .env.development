# Backend Environment Variables for Development
# This file is for local development only
# For production (Render), set these in Render dashboard

# CORS - Frontend URL (for local dev, use localhost)
CLIENT_URL=http://localhost:3000

# Session secret - use a random string in production
SESSION_SECRET=kambaz-dev-secret-change-in-production

# Environment type
SERVER_ENV=development

# Server port (optional, defaults to 4000)
PORT=4000

# SERVER_URL is only needed in production, leave empty for development
# SERVER_URL=
